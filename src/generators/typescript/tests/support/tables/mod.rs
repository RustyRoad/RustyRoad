//! Fixture tables covering the column shapes generation must handle.

mod posts;
mod users;

pub(in crate::generators::typescript::tests) use posts::posts;
pub(in crate::generators::typescript::tests) use users::users;
